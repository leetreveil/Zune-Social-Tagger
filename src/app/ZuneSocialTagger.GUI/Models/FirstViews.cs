namespace ZuneSocialTagger.GUI.Models
{
    /// <summary>
    /// Defines the available views that can be loaded at startup
    /// </summary>
    public enum FirstViews
    {
        WebAlbumListViewModel,
        SelectAudioFilesViewModel
    }
}