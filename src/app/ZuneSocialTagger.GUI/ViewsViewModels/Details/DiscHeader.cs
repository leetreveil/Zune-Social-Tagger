namespace ZuneSocialTagger.GUI.ViewsViewModels.Details
{
    /// <summary>
    /// Simple container around a string so we can use a DataTemplate in the DetailsView.xml to display the DiscNumber as a header
    /// </summary>
    public class DiscHeader
    {
        public string DiscNumber { get; set; }
    }
}