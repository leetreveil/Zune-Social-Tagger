<UserControl x:Class="ZuneSocialTagger.GUIV2.Controls.InlineZuneMessage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >

    <UserControl.Resources>
        <ResourceDictionary Source="../Resources.xaml" />
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard Storyboard="{StaticResource SizeFadeInOut}"/>
        </EventTrigger>
    </UserControl.Triggers>
    
        <StackPanel x:Name="WarningBox" Height="0" Orientation="Horizontal" HorizontalAlignment="Left" >
            <Image x:Name="imgError" Margin="0,0,10,0" Width="16" Height="16"  />
            <TextBlock x:Name="tbError" />
        </StackPanel>
 
</UserControl>
