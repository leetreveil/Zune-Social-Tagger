<UserControl x:Class="ZuneSocialTagger.GUIV2.Views.SearchHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             xmlns:views="clr-namespace:ZuneSocialTagger.GUIV2.Views">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <views:ExpandedAlbumDetailsView 
            VerticalAlignment="Top" 
            DataContext="{Binding AlbumDetails}" />
        
        <views:SearchBarView Margin="20,0,0,0"
            VerticalAlignment="Top" 
            HorizontalAlignment="Right"  
            DataContext="{Binding SearchBar}"
            Grid.Column="2"  />
    </Grid>
</UserControl>
