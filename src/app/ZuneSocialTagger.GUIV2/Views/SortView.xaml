<UserControl x:Class="ZuneSocialTagger.GUIV2.Views.SortView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary Source="../Resources.xaml" />
    </UserControl.Resources>
    
    <Grid>
        <StackPanel Visibility="{Binding SortOrder,Converter={StaticResource SortOrderToVisibilityConverter}}" Orientation="Horizontal">

            <TextBlock Text="SORTED BY " FontFamily="Assets/Fonts/#Zegoe Caps" FontWeight="Bold" 
                       FontSize="12" Foreground="#FFA2ABAA" />

            <Button Command="{Binding SortCommand}"
                    FontFamily="Assets/Fonts/#Zegoe Caps" FontWeight="Bold" 
                    FontSize="12" Style="{StaticResource AboutSettingsButton}" 
                    Content="{Binding Path=SortOrder, Converter={StaticResource SortOrderToTextConverter}}"/>
            
        </StackPanel>

    </Grid>
</UserControl>
