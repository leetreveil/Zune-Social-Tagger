<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Class="ZuneSocialTagger.GUIV2.Views.SelectAudioFilesView"
	x:Name="Window">

    <UserControl.Resources>
        <ResourceDictionary Source="../Resources.xaml" />
    </UserControl.Resources>
	
	<Grid x:Name="LayoutRoot">
		<Grid.RowDefinitions>
            <RowDefinition/>
			<RowDefinition/>
		</Grid.RowDefinitions>
        
		<Grid Grid.Row="1" VerticalAlignment="Top" Height="30">
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
				<Button Command="{Binding FromFilesCommand}" HorizontalAlignment="Center" Grid.Column="2">Select Audio Files</Button>
		</Grid>
        
        <TextBlock HorizontalAlignment="Center" Height="30" Text="Select the album you want linking to the zune social" />

	</Grid>
</UserControl>