<Window x:Class="ZuneSocialTagger.GUIV2.Views.SuccessView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="SuccessView" 
    Style="{DynamicResource DefaultWindowStyle}"  
    WindowStartupLocation="CenterScreen"
    FontFamily="/ZuneSocialTagger.GUI;Component/Assets/Fonts/#Zegoe UI - U"
    xmlns:views="clr-namespace:ZuneSocialTagger.GUIV2.Views" MouseLeftButtonDown="Window_MouseLeftButtonDown"
    Width="600" Height="200">

    <Window.Resources>
        <ResourceDictionary Source="../Resources.xaml" />
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--HEADER-->
        <TextBlock  FontSize="26.667" Text="SUCCESS"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <views:WebsiteAlbumMetaDataView DataContext="{Binding AlbumDetailsFromFile}"/>
            <Image Margin="10,10,10,10" Grid.Column="1" Source="../Assets/arrow.png"/>
            <views:WebsiteAlbumMetaDataView DataContext="{Binding AlbumDetailsFromWebsite}" Grid.Column="2"/>
        </Grid>
        
        <TextBlock Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"  Text="This album should now be correctly linked to the zune social." FontSize="13" />
        <Button Height="25" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="60" Grid.Row="2" Content="OK" Click="Button_Click" />
    </Grid>
</Window>
