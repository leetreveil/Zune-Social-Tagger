<local:DraggableWindow
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:ZuneSocialTagger.GUIV2.Views" 
    xmlns:local="clr-namespace:ZuneSocialTagger.GUIV2" x:Class="ZuneSocialTagger.GUIV2.ZuneWizardDialog" 
    SizeToContent="Height"
    Title="Zune Social Tagger" 
    WindowStartupLocation="CenterScreen"
    Style="{DynamicResource DefaultWindowStyle}" 
    Icon="Assets/zuneicondark.ico">
    
    <Window.Resources>
        <ResourceDictionary Source="Resources.xaml" />
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

    	<local:ZuneHeaderGradient/>
        
        <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">  
            <Button Margin="0,0,9,0" Style="{DynamicResource MinimizeButton}" VerticalAlignment="Bottom" Width="10" Height="2" Click="Minimize_Click"/>
            <Button ToolTip="Close" Width="10" Height="10" VerticalAlignment="Bottom" Grid.Column="1" Style="{StaticResource CloseButton}" Click="Close_Click" />
        </StackPanel>

        
        <views:ZuneWizardView Grid.Row="1" Grid.ColumnSpan="2"/>
    </Grid>

   
</local:DraggableWindow>